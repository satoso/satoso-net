---
layout: default
title: 演奏経歴
---
#concerts
  h1 演奏経歴

  dl.concerts
    | {% for concert in site.data.concerts %}
    dt
      span.date {{ concert.date | date:"%Y.%m.%d" }}
      br
      dfn
        span.title {{ concert.name }}
    dd 
      p.place @ {{ concert.place }}

      | {% if concert.credit %}
      p.credit
        | {%for s in concert.credit %}
        | {{ s[0] }}: {{ s[1] }}{% unless forloop.last %}, {% endunless %}
        | {% endfor %}
      | {% endif %}

      | {% if concert.note %}
      p.note {{ concert.note }}
      | {% endif %}

      ul
        | {% for number in concert.program %}
        li
          / アンコール
          | {% if number.encore %}［アンコール］ {% endif %}
          / 作曲者・編曲者
          | {% if number.comp %}
          | {{ number.comp }}
          | {% if number.arr %}（{{ number.arr | join:', '}}編）{% endif %}
          | {% else %}
          | {{ number.arr | join:', '}}編
          | {% endif %}
          / 区切り
          | ／
          / 曲名
          | {{ number.name }}
          / 抜粋
          | {% if number.excerpts %}
          ul
            | {% for excerpt in number.excerpts %}
            li
              | {{ excerpt }}
            | {% endfor %}
          | {% endif %}
        | {% endfor %}
    | {% endfor %}

